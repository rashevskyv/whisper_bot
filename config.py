# Налаштування GPT для різних сценаріїв
GPT_SETTINGS = {
    # Сценарій постобробки: використовується для виправлення граматики та покращення тексту
    'postprocess': {
        'model': 'gpt-4o-mini',    # Використовуємо GPT-3.5 для швидкості та ефективності
        'max_tokens': 1500,        # Максимальна довжина відповіді
        'temperature': 0.5         # Нижча температура для більш консервативних змін
    },
    # Сценарій резюмування: створення коротких підсумків довгих текстів
    'summarize': {
        'model': 'gpt-4o',         # Використовуємо GPT-4 для кращого розуміння та узагальнення
        'max_tokens': 150,         # Обмежуємо довжину резюме
        'temperature': 0.7         # Середня температура для балансу між креативністю та точністю
    },
    # Сценарій переписування: зміна стилю тексту зі збереженням змісту
    'rewrite': {
        'model': 'gpt-4o',         # GPT-4 для кращого розуміння контексту та стилю
        'max_tokens': 1500,        # Дозволяємо досить довгі відповіді
        'temperature': 0.7         # Середня температура для творчого переписування
    },
    # Сценарій запиту: загальні відповіді на запитання користувача
    'query': {
        'model': 'gpt-4o',         # GPT-4 для найбільш точних та інформативних відповідей
        'max_tokens': 1500,        # Дозволяємо досить довгі відповіді
        'temperature': 0.7         # Середня температура для балансу між точністю та креативністю
    },
    # Сценарій аналізу контенту: аналіз тексту та зображень
    'analyze_content': {
        'model': 'gpt-4o',         # Використовуємо GPT-4 з можливістю аналізу зображень
        'max_tokens': 1000,        # Обмежуємо довжину аналізу
        'temperature': 0.7         # Середня температура для балансу між деталізацією та узагальненням
    }
}

# Налаштування користувача за замовчуванням
DEFAULT_USER_SETTINGS = {
    'ENABLE_POSTPROCESSING': False,        # Вимкнено за замовчуванням для економії ресурсів
    'ENABLE_SUMMARIZATION': True,          # Увімкнено, корисно для довгих повідомлень
    'ENABLE_REWRITING': True,              # Увімкнено, корисно для покращення стилю
    'ENABLE_VIDEO_PROCESSING': False,      # Вимкнено за замовчуванням, може вимагати багато ресурсів
    'ENABLE_VIDEO_NOTE_PROCESSING': True,  # Увімкнено, корисно для коротких відео-повідомлень
    'LANGUAGE': 'uk',                      # Українська мова за замовчуванням
    'USE_GPT4': True,                      # Використовуємо GPT-4 за замовчуванням для кращої якості
    'CONTEXT_DURATION': 1,                 # Тривалість зберігання контексту розмови (в годинах)
    'ENABLE_CONTEXT': True                 # Увімкнено збереження контексту для послідовності діалогу
}

# Повідомлення для різних сценаріїв
MESSAGES = {
    # Повідомлення для сценарію постобробки
    'postprocess': {
        'system': 'Ви є помічником, який допомагає виправляти текст.',
        'user': "Виправте граматичні помилки в цьому тексті, але не змінюйте його зміст. Починай текст одразу:\n\n{text}"
    },
    # Повідомлення для сценарію резюмування
    'summarize': {
        'system': 'Ви є помічником, який допомагає створювати резюме тексту.',
        'user': "Зробіть стисле резюме наступного тексту:\n\n{text}"
    },
    # Повідомлення для сценарію переписування
    'rewrite': {
        'system': 'Ви є помічником, який допомагає переписувати текст у іншому стилі.',
        'user': "Перепишіть цей текст, зберігаючи основний зміст, але змінюючи стиль та формулювання:\n\n{text}"
    },
    # Повідомлення для сценарію запиту
    'query': {
        'system': 'Ви є корисним асистентом.',
        'user': "{text}"
    },
    # Повідомлення для сценарію аналізу контенту
    'analyze_content': {
        'system': "Ви - помічник, здатний аналізувати текст та зображення.",
        'user_text': "{text}",
        'user_image': "Аналіз цього зображення:"
    }
}